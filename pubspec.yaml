name: luna
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  html: ^0.15.4
  provider: ^6.1.2
  synchronized: ^3.1.0+1
  
  url_launcher: ^6.2.6
  #video
  webview_windows: ^0.4.0
  video_player: ^2.8.6
  chewie: ^1.8.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
